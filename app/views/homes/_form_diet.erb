<div class="item" style="padding: 15px 10% 120px 10%;">
  <legend>食事管理</legend>

  <%= form_for(@protein, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @protein.errors.any? %>
  <div class="alert alert-danger alert-dismissable" role="alert">
    <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
    <h4><%= pluralize(@protein.errors.count, "error") %> prohibited this goal from being saved:</h4>
    <ul>
      <% @protein.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

  <div class="form-group">
    <%= f.label "日付", class: "col-sm-2 control-label" %>
    <div class="col-sm-10 form-inline">
      <%= f.date_select :diary_date, {}, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label "タンパク質", class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :protein_intake, placeholder: "摂取したタンパク質 [g]", class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit "日記をつける", class: "btn btn-primary" %>
    </div>
  </div>

  <% end %>
</div>
